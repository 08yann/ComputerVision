model parameters:
  img_channels: 3
  img_size: 128
  encoder_kernels: [7,3,3,3]
  encoder_strides: [2,2,2,2]
  maxpool_stride: 2
  avgpool: True
  maxpool_stride: 2
  encoder_size_blocks: 1
  encoder_channels: [64,128,256,512]
  latent_dim: 512
  decoder_channels: [512, 256, 128, 64]
  decoder_nb_blockconvs: 0